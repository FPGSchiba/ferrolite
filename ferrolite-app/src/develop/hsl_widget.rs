//! HSL swatch row + per-band sliders — stub until Task 12.
// TODO(Task 12): implement the HSL widget

use crate::develop::adjustment_panel::EditOutcome;
use ferrolite_pipeline::OpStack;

pub fn show(_ui: &mut egui::Ui, _stack: &OpStack, _band: &mut usize) -> Option<EditOutcome> {
    None
}
